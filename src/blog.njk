---
layout: layouts/base.njk
title: 블로그
---
<section class="page-hero">
  <p class="eyebrow">Archive</p>
  <h1>블로그 & Devlog</h1>
  <p class="muted">최신순 목록과 카테고리별 모아보기.</p>
  <div class="hero-actions">
    <a class="text-link" href="{{ '/' | url }}">홈으로 돌아가기</a>
  </div>
</section>

<section class="layout-two archive">
  <div class="feed">
    <div class="post-list archive-list">
      {% for post in collections.posts %}
        <a class="post-item compact" href="{{ post.url | url }}">
          <div class="post-item-top">
            <span class="badge">{{ post.data.category | default('notes') }}</span>
            <span class="post-meta">{{ post.date | readableDate }} · {{ post.data.readingTime or '읽기' }}</span>
          </div>
          <div class="post-item-title">{{ post.data.title }}</div>
          <div class="post-item-excerpt muted">{{ post.data.excerpt }}</div>
        </a>
      {% endfor %}
    </div>
  </div>
  <aside class="sidebar">
    <div class="sidebar-card">
      <div class="card-title">카테고리</div>
      <ul class="category-list">
        {% set labels = {
          'devlog': 'Devlog',
          'freelance': 'Freelance',
          'games': 'Games',
          'notes': 'Notes'
        } %}
        {% for key, posts in collections.byCategory %}
          <li>
            <a href="#{{ key }}">{{ labels[key] or key }}</a>
            <span class="count">{{ posts | length }}</span>
          </li>
        {% endfor %}
      </ul>
    </div>
    {% include "ad-slot.njk" %}
  </aside>
</section>

<section class="category-panels">
  <div class="section-heading">
    <div>
      <p class="eyebrow">Categories</p>
      <h2>주제별 모아보기</h2>
    </div>
  </div>
  <div class="category-grid">
    {% set labels = {
      'devlog': 'Devlog',
      'freelance': 'Freelance',
      'games': 'Games',
      'notes': 'Notes'
    } %}
    {% for key, posts in collections.byCategory %}
      <div class="category-block" id="{{ key }}">
        <div class="block-head">
          <div class="pill">{{ labels[key] or key }}</div>
          <h3>{{ labels[key] or key }}</h3>
        </div>
        <div class="stack">
          {% for post in posts %}
            <a class="stack-item" href="{{ post.url | url }}">
              <div>
                <div class="muted">{{ post.date | readableDate }}</div>
                <div class="stack-title">{{ post.data.title }}</div>
                <div class="muted">{{ post.data.excerpt }}</div>
              </div>
              <span class="tag">{{ post.data.readingTime or '읽기' }}</span>
            </a>
          {% endfor %}
        </div>
      </div>
    {% endfor %}
  </div>
</section>
