---
layout: layouts/base.njk
title: 외주 작업
description: "프로젝트 수행 기록"
---
<section class="freelance-page">
  <header class="page-header">
    <h1>외주 작업</h1>
    <p class="lede">프로젝트 수행 기록</p>
  </header>

  <div class="post-list">
    {% for job in studio.freelance.jobs %}
      {% set cardSlug = job | cardSlug %}
      <article class="post-item">
        <a href="{{ ('/cards/' + cardSlug + '/') | url }}">
          <div class="post-info">
            <span class="post-category">외주</span>
            <h2 class="post-title">{{ job.client }}</h2>
            <p class="post-excerpt">
              {% if job.work and job.notes %}
                {{ job.work }} · {{ job.notes }}
              {% elif job.work %}
                {{ job.work }}
              {% elif job.notes %}
                {{ job.notes }}
              {% endif %}
            </p>
            {% if job.year %}
              <span class="post-date">{{ job.year }}</span>
            {% endif %}
          </div>
        </a>
      </article>
    {% endfor %}
  </div>
</section>
